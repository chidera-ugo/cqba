import { DatePickerValue } from 'types/commons';

export const initialValues = {
  title: '',
  expiryDate: {} as DatePickerValue,
  amount: '',
  description: '',
  allocation: '',
  expires: false,
  threshold: false,
};
