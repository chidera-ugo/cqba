import { DatePickerValue } from 'types/Common';

export const initialValues = {
  title: '',
  expiryDate: {} as DatePickerValue,
  amount: '',
  description: '',
  allocation: '',
  expires: false,
  threshold: false,
};
